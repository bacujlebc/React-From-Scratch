import React, { Fragment } from 'react';
import Layout from '../Layout';

const Home = () => {
	return (
		<Fragment>
			<Layout>
				<div>Home</div>
			</Layout>
		</Fragment>
	);
};

export default Home;
